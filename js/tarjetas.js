// Obtener el elemento con la clase "tarjetas"
var tarjetas = document.querySelector('.tarjetas');

// Cambiar el color de fondo de las tarjetas
tarjetas.style.backgroundColor = 'lightblue';

// Cambiar el tamaño del borde de las tarjetas
tarjetas.style.borderWidth = '2px';

// Cambiar el color del borde de las tarjetas
tarjetas.style.borderColor = 'blue';

// Cambiar la sombra de las tarjetas
tarjetas.style.boxShadow = '4px 4px 10px rgba(0, 0, 0, 0.1)';
